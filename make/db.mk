# db 

# Because DB's need to be installed with the code, we need toolng to install and run locally, but also to the cloud.
# And we need GUI tools.

# dep 
# tools, etc

# https://github.com/dosco/graphjin
# postres
# sqlite
# others

db-sqlite-dep:
	brew install sqlite
	brew install --cask db-browser-for-sqlite